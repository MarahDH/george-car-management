import CustomersPage from './CustomersPage'

/** Customers who stopped coming to the center — archived from their profile page. */
export default function ArchivedCustomersPage() {
  return <CustomersPage archived />
}
